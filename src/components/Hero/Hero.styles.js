import styled from "styled-components";

export const StyledHero = styled.header`
	padding-bottom: 5rem;

	img {
		width: 100%;
		height: auto;
	}

	.text-box {
		text-align: center;
	}
	.title {
		font-size: 2.3rem;
		color: var(--dark-blue);
		margin-bottom: 0.5rem;
	}
	.subtitle {
		font-size: 1rem;
		font-weight: 400;
		color: var(--gray-blue);
		line-height: 1.5;
		margin-bottom: 2rem;
	}
`;
