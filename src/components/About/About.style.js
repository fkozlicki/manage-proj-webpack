import styled from "styled-components";

export const StyledAbout = styled.div`
	.text-box {
		text-align: center;
	}

	.title {
		font-size: 1.9rem;
		line-height: 1.5;
		color: var(--dark-blue);
		margin-bottom: 1rem;
	}
	.subtitle {
		font-size: 0.9rem;
		font-weight: 500;
		line-height: 2;
		color: var(--gray-blue);
		margin-bottom: 3rem;
	}
`;
